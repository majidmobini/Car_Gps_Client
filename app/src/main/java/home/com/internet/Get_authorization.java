package home.com.internet;

public class Get_authorization {
	public String get_auth()
	{
	//	return "Basic " + Base64.encodeToString((""+":"+"").getBytes(), Base64.NO_WRAP);
	//	return "Basic " + Base64.encodeToString((MainActivity.user+":"+MainActivity.password).getBytes(), Base64.NO_WRAP);
		//return "Basic " + Base64.encodeToString((MainActivity.user+":"+MainActivity.password).getBytes(), Base64.NO_WRAP);
		return "pich pich";
	}

}
